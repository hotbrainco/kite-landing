<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" type="image/png" href="/logo.png">
    <link rel="shortcut icon" type="image/png" href="/logo.png">
    <title>{{ title or "Kite - The Website & Communications Platform for Churches" }}</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/styles.css">
    <script src="https://unpkg.com/lucide@latest"></script>
    <!-- Apply theme immediately to avoid flash of incorrect styles -->
    <script>
        (function() {
            const savedTheme = localStorage.getItem('theme') || 'light';
            if (savedTheme === 'light') {
                document.documentElement.classList.add('light-theme');
            }
        })();
    </script>
    <script>
        window.stripePublishableKey = "{{ site.stripePublishableKey | default('MISSING_KEY') }}";
        window.isTestMode = {{ site.isTestMode }};
        if (!window.stripePublishableKey || window.stripePublishableKey === "MISSING_KEY") {
            console.error("Stripe publishable key is missing!");
        }
    </script>
</head>
<body>
    {% include "header.njk" %}
    
    <main>
        {{ content | safe }}
    </main>
    
    {% include "footer.njk" %}

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/script.js"></script>
    <script>
        lucide.createIcons();
    </script>
    
    {{ scripts | safe }}

</body>
</html>
